<div class="text-field">
    <label for="{!! $name !!}" class="field__label">
        {!! $slot!!}
    </label>
    <input type="text" name="{{$name}}" id="{{$id}}" value="{{$value}}" class="field__input" placeholder="{{$placeholder}}"
           aria-required="true">

</div>
